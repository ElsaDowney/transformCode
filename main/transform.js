function turnToZipcode(inputs) {

    const isText = checkText(inputs);
}

function checkText(inputs) {
    const text =inputs.replace('-','');
    const length = text.length;
    console.log(text);
}